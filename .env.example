# Nextcloud MCP Gateway Configuration
# Target Nextcloud instance endpoint
NC_URL=http://127.0.0.1:8080

# Production / Public domain endpoint (optional)
NC_PUBLIC_URL=https://nc.shtab-ai.ru

# Nextcloud authentication credentials
NC_USER=zavlab
NC_APP_PASSWORD=your_nextcloud_app_password_here

# Request timeout in seconds
NC_TIMEOUT=30

# Enforce TLS certificate verification (disable only for local dev with self-signed certs)
NC_VERIFY_SSL=true
